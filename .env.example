DATABASE_URL=
BETTER_AUTH_SECRET=
NEXT_PUBLIC_VERCEL_APP_CLIENT_ID=
VERCEL_APP_CLIENT_SECRET=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: override the app origin for a custom production domain.
BETTER_AUTH_URL=

# Optional: enable hosted Vercel Connect integrations.
SLACK_CONNECTOR=
LINEAR_CONNECTOR=
NOTION_CONNECTOR=
SENTRY_CONNECTOR=

# Supabase quotes database (read/written by the eve agent tools).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Postgres connection string (Supabase pooler) used for read-only analytics SQL.
# Required for the analyst subagent. Find it in the Supabase dashboard:
# Settings > Database > Connection string (pooler, port 6543).
SUPABASE_DB_URL=
